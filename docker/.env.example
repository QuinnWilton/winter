# Winter configuration
WINTER_PDS_URL=https://razorgirl.diy
WINTER_HANDLE=winter.razorgirl.diy
WINTER_APP_PASSWORD=your-app-password-here

# Claude API token (required for daemon)
CLAUDE_CODE_OAUTH_TOKEN=claude-code-token-here

# Web UI URL (for approval links in DMs)
WINTER_WEB_URL=https://winter.razorgirl.diy

# Firehose URL for real-time updates (optional)
# WINTER_FIREHOSE_URL=wss://bsky.network

# Secrets file path (auto-set in Docker, override for local development)
# WINTER_SECRETS_PATH=/home/winter/.config/winter/secrets.json

# Workspace directory (mounted into container for custom tools with filesystem access)
# Create ~/.winter-workspace on host before starting: mkdir -p ~/.winter-workspace
# WINTER_WORKSPACE=/home/winter/workspace

# Fast-forward mode: skip all existing notifications/DMs on startup
# Set to any non-empty value (e.g., "1" or "true") to enable
# Useful when deploying for the first time or recovering from downtime
# WINTER_FAST_FORWARD=1

# Bootstrap configuration (used by `docker compose --profile bootstrap run --rm bootstrap`)
# WINTER_OPERATOR_DID is required - the DID of the human operator controlling this instance
WINTER_OPERATOR_DID=did:plc:your-operator-did-here
# WINTER_VALUES=intellectual honesty,genuine curiosity,thoughtful engagement
# WINTER_INTERESTS=distributed systems,philosophy of mind,creative writing
# WINTER_SELF_DESCRIPTION=I am Winter, an autonomous agent exploring the fediverse.
